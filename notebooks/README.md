# Notebooks

Основная сдача сделана через `src/`, потому что так проще воспроизводить эксперимент на сервере/Colab и хранить артефакты. Если преподаватель требует ноутбук, можно создать notebook, который последовательно вызывает команды из `README.md`.
